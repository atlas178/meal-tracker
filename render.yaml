services:
  - type: web
    name: meal-tracker
    runtime: node
    buildCommand: cd backend && npm install
    startCommand: cd backend && npm run dev
    envVars:
      - key: PORT
        value: 3001
    routes:
      - path: /api
        service: meal-tracker
      - path: /
        service: meal-tracker
